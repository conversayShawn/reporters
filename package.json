{
  "name": "reporters",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "merge": "npx cypress run && npx mochawesome-merge cypress/report/mochawesome-report/*.json > cypress/report/output.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.3.0",
    "mochawesome": "^6.3.1"
  }
}
